alias builtin 外部命令  顺序由高到低
which -a 强迫搜索path的目录 搜索命令在哪
which --skip-alias COMMEND 跳过别名 显示路径
help 直接看内部命令有多少个
enable -n COMMDEND 禁用某个命令
enable COMMDEND 启用某个命令
bc 计算器  ibase的意思就是inputbase  输入的是几进制  obase就是输出几进制
lsblk 看块设备
hexdump 看二进制文件
cat 回车的话  输入什么显示什么  ctrl+d正常退出 ctrl+c强制退出
ls -1  1是数字1  竖排显示
whatis 文件或者命令 可以看到其man信息或者其他
last   登陆用户的信息历史
proc的信息是0，他的东西都是从内存中读取的
HISTORYCONTRAL=ignorespace
who是看有多少个用户登陆了 
whoami是看自己是谁 who am i  可以看更加详细的
清空文件用>     比如 > /etc/passwd 或者 cat /dev/null > /etc/passwd
chattr +i 文件名   就可以锁定文件
seq  sequence    seq  第一个 步长 最后一个  生成一个序列
readlink 可以查看一个软连接指向的物理路径。
reset 出现乱码了  比如cat了乱码了 恢复的时候使用 恢复设置
cat -b 可以在前面加行号 但是第一行的那个空行没有加
cat -n  包括那个第一行那个空行也会加行号的
cat -A 看不见的符号也能看到 比如回车什么的
makewhatis可以更新数据库 whatis的数据库
tail -n0 -f 文件名 &  在后台执行监控文件的变化，只是显示新的内容而且有了新变化就会显示
fset421
可以使用 lsattr 命令查看是否为一个文件或目录设置了不可变标志（或其他任何属性）。要让文件不可变，可以使用 chattr 命令和 -i 标志。
eject  在虚拟机上连接物理的光盘  可以弹出光驱
whach 可以在创建raid的时候 可以一秒看一次  看到动态的读条信息
mdadm --zero-superblock /dev/sd# 清除磁盘上的raid信息
resize2fs 同步拓展文件系统 
init 1 5 0 6  单用户 图像界面 关机 
e2label /dev/sda  labelname    可以查看sda的卷标

setenforce 0  可以关闭selinux   使用getenforce可以查看selinux的开关 

